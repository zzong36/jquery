$(document).ready(function(){
    // $('h1').show(3000)
    // $('h1').slideUp(1000)
    
    // 3초 동안 "Hello JQUERY"라는 문장을 보인 후 글자색을 파란색으로 
    // 변경한 후 슬라이드 효과로 올라갔다 내려갔다를 수행하자
    $('h1').show(3000).css('color', 'blue')
})